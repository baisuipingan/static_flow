# Tailwind CLI 二进制（使用 npm 管理，无需提交）
tailwindcss
tailwindcss.exe

# Node.js（构建时依赖）
# 保留 package-lock.json 用于版本锁定和 CI 缓存
node_modules/
pnpm-lock.yaml
yarn.lock
/static/gpt2api/
/gpt2api-app/node_modules/
/gpt2api-app/dist/
/gpt2api-app/tsconfig.tsbuildinfo

# trunk serve dev output (separate from dist/ used by self-hosted)
dist-dev/
