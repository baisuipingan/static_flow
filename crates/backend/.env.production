# StaticFlow Backend Production Configuration

# Environment
RUST_ENV=production

# Server Configuration
PORT=9999
BIND_ADDR=127.0.0.1

# LanceDB Paths (adjust according to your server setup)
LANCEDB_URI=/opt/staticflow/data/lancedb

# Optional CORS allowlist (comma-separated)
# ALLOWED_ORIGINS=https://acking-you.github.io,https://your-frontend-domain.com

# Logging
RUST_LOG=info
